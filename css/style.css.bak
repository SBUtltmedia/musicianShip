/* .chords{
  width: 25%;
  height: 60%;
  background-color: #0000ff;

}

#chordBox {
  display: flex;
 flex-direction: column;
  flex-wrap: wrap;
}

.chords:nth-child(2n) {
    flex-basis: 100%;
} */
/* General settings for the whole page */
body {
  background-color: black;
  font-family: 'Montserrat', sans-serif;
  display: none;
  flex-direction: column;

  /* justify-content: center; */
  align-items: center;
  /* padding: 0.3rem; */
}


p {
  margin: auto;
  -webkit-touch-callout: none; /* iOS Safari */
      -webkit-user-select: none; /* Safari */
       -khtml-user-select: none; /* Konqueror HTML */
         -moz-user-select: none; /* Firefox */
          -ms-user-select: none; /* Internet Explorer/Edge */
              user-select: none; /* Non-prefixed version, currently*/
  /* text-align: center; */
}

#topMenu{
  display: flex;
  flex-direction: row;


  /* width: 80%; */
}

/* This is a general theming for background square enveloping elements */
.bg-square {
  display: flex;
  /* flex-direction: column; */
  background-color: #251e38;
  border-radius: 0.3rem;
  padding: 1rem;
  margin: 0.2rem;
  box-shadow: inset 0px 0px 8px 2px rgba(0, 0, 0, 0.9);
  color: white;
  justify-content: center;
  /* text-transform: uppercase; */
}

/* #rows div div { */
.bg-square div div {
  /* background-color: #787582; */
  border-radius: 0.3rem;
  /* box-shadow: inset 0px 0px 10px 3px rgba(255, 255, 255, 0.3); */
  margin: auto;
  display: flex;
  margin: 0.3rem;
  width: 5rem;
  height: 2rem;
  color: white;
  text-shadow: 0 0 10px #000000;
}


.btnClicked {
  /* the problem is that this is competing with #colums, which has higher priority */
  background-color: #7d2faf;
  border-radius: 0.3rem;
  /* padding: 1rem; */
  margin: 0.2rem;
  box-shadow: inset 0px 0px 8px 2px rgba(0, 0, 0, 0.9);
  /* justify-content: center; */
}


.btnReady { */
   background-color: #655b6d; */
  /* background-color: #787582; */
   padding: 5 rem; */
   box-shadow: inset 0px 0px 10px 3px rgba(255, 255, 255, 0.2); */
 }

 .btnHighlighted { */
   the problem is that this is competing with #colums, which has higher priority */
   background-color: #888888; */
   border-radius: 0.3rem; */
   padding: 1rem; */
   margin: 0.2rem; */
   box-shadow: inset 0px 0px 2px 1px rgba(1, 1, 1, 0.9); */
   justify-content: center; */
 } */


.menuBtn {
  display: flex;
  background-color: rgba(255,255,255,0.2);
  /* background-color: rgba(1,1,1,0.9); */
  padding: 0.6rem;
  margin: 0.3rem;
  text-transform: uppercase;
  border-radius: 0.3rem;
}

.textbox {
  /* display: flex; */
  flex-direction: column;
  /* background-color: #31274c; */
  /* border-radius:0.3rem; */
  /* padding: 1rem; */
  /* margin: auto; */
  /* margin:0.3rem; */
  /* width:80%; */
  /* height:2rem; */
  /* color: white; */
  /* justify-content: center; */
  text-transform: uppercase;
}

#answerbox{
  width: 50%;
  height: 1.5rem;
}

/*
#title div {
  text-align: right;
} */
#rows {
  display: flex;
  padding: 1rem;
}

#rows>div {
  flex: 1;
}
